import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nexa Solutions | ERPNext & Frappe Implementors",
  description: "Nexa Solutions - Your trusted ERPNext and Frappe implementors. We deliver comprehensive ERP solutions to businesses across multiple industries, helping you transform operations and achieve growth.",
  keywords: "ERPNext, Frappe, ERP solutions, business automation, Kenya, Nairobi",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Navbar />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
